{
  "name": "helloworld-service",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/backend/helloworld-service/src",
  "tags": [],
  "root": "apps/backend/helloworld-service",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "cd {root} && ./gradlew build"
        ]
      }
    },
    "deploy": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "cd {root} && sam deploy"
        ]
      }
    }
  }
}
